<div class="section cards">
  <div class="container">
    <div class="row">
      <div class="eight columns offset-by-two">
        <div class="spacer"></div>
        <div class="spacer"></div>
        <h2 class="centered-text">
          Current opportunities to volunteer your expertise:
        </h2>
        <div class="spacer"></div>
        <% @projects.each do |project| %>
          <% unless project.id == 1 || project.active == false || project.organization.approved == false %>
              <div class="card">
                <div class="content">
                  <h3 class="clamped-text one-line">
                    <%= link_to project.title, project %>
                  </h3>
                  <div class="divider"></div>
                  <p class="clamped-text four-lines">
                    <%= project.description %>
                  </p>
                  <% if project.required_roles %>
                      <p>
                        Team Needs: <%= project.required_roles %>
                      </p>
                  <% end %>
                  <%= link_to "View Project", project, class: "button-secondary" %>
                </div>
              </div>
            <% end %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
